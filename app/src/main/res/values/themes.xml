<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light theme without an ActionBar, because each screen has its own Toolbar. -->
    <style name="Theme.KotlinTextEditor" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="actionBarTheme">@style/ThemeOverlay.Material3.Dark.ActionBar</item>
        <item name="actionBarPopupTheme">@style/ThemeOverlay.Material3.Light</item>
    </style>
</resources>
